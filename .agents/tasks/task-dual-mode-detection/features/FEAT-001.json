{
  "id": "FEAT-001",
  "type": "feat",
  "description": "Add dual detection modes (Snapshot and Contador), face tracking/deduplication, live stats card, poem messages for snapshot mode, and CSV/Excel export from dashboard",
  "status": "completed",
  "steps": [
    "1. Create src/lib/poems.ts - Define poem paragraphs mapped by EmotionType",
    "2. Update src/app/page.tsx - Add mode selection UI and live stats card",
    "3. Refactor src/components/FaceDetector.tsx into dual-mode with face tracking",
    "4. Update src/app/globals.css - Add zoom animation keyframes",
    "5. Update src/app/dashboard/page.tsx - Add CSV export button",
    "6. Update src/app/api/dashboard/route.ts - Add format=csv support"
  ],
  "acceptance_criteria": [
    "Mode selection (Snapshot/Contador) is visible and functional on the main page before starting detection",
    "In Snapshot mode, a photo is captured and displayed with zoom animation when emotions are detected (>=50% threshold for groups)",
    "Snapshot mode shows a poem paragraph matching the dominant emotion instead of the standard message",
    "In Contador mode, only counting occurs - no photo overlay, live stats shown",
    "Face deduplication prevents counting the same person multiple times while they remain in frame",
    "A prominent stats card at the top shows the unique person count during detection",
    "Dashboard has a functional CSV export button that downloads captured data",
    "npm run build completes without errors"
  ],
  "verification": [
    "Run: export NVM_DIR=\"$HOME/.nvm\" && [ -s \"$NVM_DIR/nvm.sh\" ] && . \"$NVM_DIR/nvm.sh\" && cd /projects/sandbox/emociones-app && npm run build"
  ],
  "blocked_reason": null,
  "findings": "All features implemented successfully. Build passes cleanly. Review fixes applied: cumulative person count preserved across tracking resets, detection loop continues during snapshot overlay, CSV export limited to 10000 rows, snapshotShownForFacesRef preserved across resets, onPersonCount wrapped in useCallback, single-face confidence threshold added."
}
